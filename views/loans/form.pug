div
  label(for="book_id") Book Id:
  input(type="integer", name="book_id" value=loan.book_id)
div
  label(for="patron_id") Patron Id:
  input(type="integer", name="patron_id" value=loan.patron_id)
div
  label(for="loaned_on") Loaned On:
  input(type="date", name="loaned_on" value=loan.loaned_on)
div
  label(for="return_by") Return By:
  input(type="date", name="return_by" value=loan.return_by)
div
  label(for="returned_on") Returned On:
  input(type="date", name="returned_on" value=loan.returned_on)
input(type="submit")